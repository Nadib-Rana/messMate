version: "3.9"

services:
  # PostgreSQL Database
  postgres:
    image: postgres:17-alpine
    container_name: messmate_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgrespassword
      POSTGRES_DB: messmate_db
    ports:
      - "5435:5432"
    volumes:
      - messmate_postgres_data:/var/lib/postgresql/data
    networks:
      - messmate_network

  # MinIO Object Storage (S3 compatible)
  minio:
    image: minio/minio:latest
    container_name: messmate_minio
    restart: unless-stopped
    ports:
      - "9000:9000" # S3 API Port
      - "9001:9001" # Web Console UI
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - messmate_minio_data:/data
    command: server /data --console-address ":9001"
    networks:
      - messmate_network

  # Mailpit - Local Email Testing Server with Web UI
  mailpit:
    image: axllent/mailpit:latest
    container_name: messmate_mailpit
    restart: unless-stopped
    ports:
      - "1025:1025" # SMTP port
      - "8025:8025" # Web UI
    networks:
      - messmate_network

networks:
  messmate_network:
    driver: bridge

volumes:
  messmate_postgres_data:
    driver: local
  messmate_minio_data:
    driver: local
